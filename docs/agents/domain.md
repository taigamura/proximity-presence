# Domain Docs

**Layout:** single-context

- Domain language and architecture overview: `CONTEXT.md` (repo root)
- Architectural decision records: `docs/adr/` (repo root)

When reading domain context, always load `CONTEXT.md` first. Check `docs/adr/` for past decisions before proposing architectural changes.
